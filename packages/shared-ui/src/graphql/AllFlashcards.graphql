query AllFlashcards($topic: String) {
  allFlashcards(
    condition: {
      topic: $topic
    }
    orderBy: PROMPT_DESC
  ) {
    nodes {
      id
      prompt
      answer
      topic
    }
  }
}
